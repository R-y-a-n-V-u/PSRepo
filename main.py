import os
import json
import time
from datetime import datetime
from PS_scraper import fetch_gen9ou_replays
from PS_json_cleaner import clean_showdown_replay

"""def main():
will call the PS scraper then take all the links and clean them using clean clean_showdown_replay()
then save them directly to database."""
